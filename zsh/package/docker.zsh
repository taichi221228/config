#!/bin/zsh

# Lima
# export DOCKER_HOST=unix://${HOME}/.lima/docker/sock/docker.sock
# export LIMA_INSTANCE=docker

alias dc='docker'
alias dcc='docker compose'
alias dccd='docker compose down'
alias dccs='docker compose start'
alias dccst='docker compose stop'
alias dccu='docker compose up'
alias dce='docker exec'
alias dcps='docker ps'
alias dcr='docker run'
alias dcs='docker start'
alias dcst='docker stop'
